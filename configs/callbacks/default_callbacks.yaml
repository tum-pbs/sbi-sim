datasetcheck:
  target: sbisim.callbacks.datasetcheck.DatasetCheck
  params:
#flowcheck:
#  target: callbacks.flowcheck.FlowCheck
#  params:
#    interrupt: False
checkpoint:
  target: sbisim.callbacks.checkpoint.Checkpoint
  params:
    savedir: ${runtime.logdir}
    key: val_loss