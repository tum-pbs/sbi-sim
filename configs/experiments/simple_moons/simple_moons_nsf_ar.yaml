name: NSF_AR

training:

  num_epochs: 100
  num_steps_per_epoch: 1000
  batch_size: &batch_size 256

optimization:

  _file: configs/optimization/default_optimization.yaml

data:

  num_features: &num_features 2
  batch_size: *batch_size
  _file: configs/datasets/simple_moons.yaml

model:

  target: flows.nsf.StackedNSF_AR
  params:
    steps: 6
    dim_flow: *num_features
    dim_conditioning: 0
    import_samples: 8

callbacks:
  _file: configs/callbacks/default_callbacks.yaml
  plots:
    target: callbacks.moons_simple.SimpleMoonDatasetPlots
    params:



